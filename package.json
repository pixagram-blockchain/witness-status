{
  "name": "witness-status",
  "version": "0.1.0",
  "description": "Live technical status of Pixagram witnesses",
  "type": "module",
  "private": true,
  "bin": {
    "witness-status": "bin/witness-status.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "smoke": "node scripts/smoke.mjs",
    "serve": "npm run build && python3 -m http.server 8000",
    "build": "node scripts/build.mjs",
    "build:check": "node scripts/build.mjs --check"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
